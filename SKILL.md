---
name: open-blue-page
description: 打开一个蓝色背景的本地网页。用于用户说“打开蓝色网页”“显示蓝色页面”或“给我一个蓝色背景页面”，支持离线预览和按要求调整蓝色色值。
---

# 打开蓝色网页

将随附的 [蓝色页面](assets/blue.html) 展示给用户。默认“蓝色网页”指蓝色背景页面；若用户指的是某个具体网站且没有给出地址，先询问网址，不猜测网站。

## 输入与环境

- 必需输入：打开蓝色页面的请求，无需账号、密钥或网络。
- 可选输入：蓝色色值、页面标题、输出目录。默认背景为 `#1565C0`，标题为“蓝色网页”。
- 依赖：支持 HTML5 和 CSS 的浏览器，以及宿主提供的文件读取与浏览器打开能力；不需要安装软件包或指定模型。

## 操作流程

1. 从本 Skill 目录解析 `assets/blue.html` 的绝对路径，不依赖当前工作目录。
2. 若未要求修改，直接使用随附页面。若要求修改，将页面复制到用户指定目录或当前工作区的输出目录，避免覆盖已有文件；只在副本中修改 `--page-blue`、`title` 和可见标题。将用户标题作为文本转义，不作为 HTML 执行。
3. 使用宿主可用的浏览器工具或文件打开能力打开页面的绝对路径或对应 `file://` URL；遵守宿主工具的实际接口。不要只输出源代码就声称已打开。
4. 若宿主禁止本地文件导航，优先使用宿主 HTML 预览；若具备命令执行能力，可从仅包含该页面的目录启动临时静态服务，绑定 `127.0.0.1` 和空闲端口，再打开页面。记录进程信息，使用结束后关闭自己启动的服务。
5. 用浏览器状态或截图确认页面加载成功：背景为指定蓝色，标题清晰，无错误页。能够检查页面时才报告“已打开”；无法检查时说明已发起打开但尚未验证。
6. 交付可点击的页面路径或本地预览地址。若没有浏览器或预览能力，提供 HTML 文件并说明用户可以双击打开，不将生成文件误报为打开成功。

## 边界与故障处理

- 页面为独立静态文件，不请求外部资源、不收集数据、不修改浏览器主页。
- 不因打开页面而发布网站、上传文件或安装扩展；发布需要用户另行提出。
- 缺少随附文件时，报告缺失路径；不要下载未知替代页面。
- 用户提供不合法的色值时，请其给出有效 CSS 色值，或说明将使用默认蓝色。不要悄悄改成另一种颜色。
- 浏览器打开失败时提供原始文件链接与简短原因，不重复启动服务或打开大量标签页。

## 示例

- “打开蓝色网页”：打开随附的默认蓝色页面，验证后交付路径。
- “打开浅蓝色页面，标题叫午后”：创建副本，将背景设为 `#B3E5FC`，采用深色文字保证可读性，并把文档和可见标题改为“午后”。
- “打开那个蓝色网站”：询问具体网址，再使用宿主浏览器导航能力。
- 无浏览器工具：交付 `blue.html`，说明已准备好文件但未自动打开。

## License

This Skill is licensed under the MIT License. The complete license text is included in [LICENSE](LICENSE).

SPDX-License-Identifier: MIT
